#include "main.h"
/**
 * mul - accepts 2 integers and returns their product
 * @a: the first integer
 * @b: the second integer
 * Return: the product of a and b
 */
int mul(int a, int b)
{
	return (a * b);
}
