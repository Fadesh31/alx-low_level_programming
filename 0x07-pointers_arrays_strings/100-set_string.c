#include "main.h"

/**
 * set_string - value of a string to a character
 * @s: pointer to the string
 * @to: the pointer to the destination character
 */
void set_string(char **s, char *to)
{
	*s = to;
}
