#include "main.h"
/**
 * _abs - entry point
 * Description: prints absolute value of an integer
 * @c: n is an integer
 * Return: int
 */
int _abs(int)
{
	if (n > 0)
	{
		return (n);
	}
	else if (n < 0)
	{
		return (-n);
	}
	else 
	{
		return (n);
	}
}
